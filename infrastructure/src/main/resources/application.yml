server:
  servlet:
    context-path: /api

spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:postgresql://${postgre.url}/${postgre.database}
    username: ${postgre.username}
    password: ${postgre.password}
